*ntf.txt*

==============================================================================
USAGE                                                              *ntf-USAGE*

ntf takes a command. `run` runs the tests and is what a bare `ntf` means, `list`
lists them without running them, and `mutation` mutation-tests the covered code
with commands of its own. A command takes exactly the options it can act on and
rejects the rest.
>
  Usage: ntf [run] [options] [spec-file-or-dir...]

  Commands:
    run (default)  run the tests and report the results
    list           list the tests without running them
    mutation       mutation-test the covered code

  Options:
    --filter=PATTERN     run only tests whose full name matches the Lua pattern
    --process-hook=FILE  run a Lua module providing setup in every process, before it loads any spec
    --global-hook=FILE   run a Lua module providing setup/teardown once around the whole run, in the launcher process
    --exclude-spec=PATH  skip a spec file or directory when discovering tests (repeatable)
    --timeout=MS         kill a worker after MS milliseconds (default: 60000; 0 disables)
    --jobs=N             max parallel nvim workers (default: cpu count)
    --test-hook=FILE     run a Lua module providing setup/teardown around each test, in its worker
    --coverage[=FILE]    measure line coverage; write the luacov.stats.out format to FILE (default: a cache file named for the working directory) and print a summary
    --exclude-code=PATH  leave a file or directory out of the code that is measured and mutated (repeatable)
    -h, --help           show this help

  With no paths, the *_spec.lua files under ./spec are used.
<
- `ntf CMD --help` prints the options of any other command, such as
  `ntf mutation --help` or `ntf mutation baseline add --help`.
- Only leading tokens name a command, so `ntf run list` is how you name a path
  that reads like a command.
- `bin/ntf` (and `bin/ntf.bat` on Windows) launches the `nvim` found on `$PATH`.
  Set `$NTF_NVIM` to a binary to run the launcher and its workers with that one
  instead. It has to be Neovim 0.12.0 or later.
- |ntf-HOOKS|, |ntf-COVERAGE| and |ntf-MUTATION-TESTING| cover the rest.

==============================================================================
WRITING SPECS                                              *ntf-WRITING-SPECS*

>lua
  -- The test API is pulled from `require("ntf")` explicitly (no global injection).
  local ntf = require("ntf")
  local describe, it, pending = ntf.describe, ntf.it, ntf.pending
  local before_each, after_each, finally = ntf.before_each, ntf.after_each, ntf.finally
  local assert = ntf.assert

  describe("group", function()
    local value
    before_each(function()
      value = 1
    end)
    after_each(function()
      value = nil
    end)

    it("does something", function()
      finally(function()
        -- runs when this test finishes, whether it passed or failed
      end)
      assert.equal(1, value)
    end)

    pending("not implemented yet")
  end)
<
A test's full name — the enclosing `describe` names and its own, joined by
spaces — has to be its own within the file. Two tests sharing one fails the
file, naming both declaration sites, rather than running either. So a test built
in a loop puts what varies into its name:

>lua
  for _, n in ipairs({ 1, 2, 3 }) do
    it("case " .. n, function()
      assert.equal(n, subject(n))
    end)
  end
<
A name is a single line: a newline in it is written as the `\n` it is, and that
written form is what a `--filter` pattern and an `invariant_spec` are matched
against. So the name a report shows is the one to write back.

The tests are declared once to plan the run and once again in each worker, so
the declarations have to come out the same both times. A file that decides what
to declare from anything the two processes do not share — what another spec left
behind in the launcher, what a hook set up in the worker, the order `pairs()`
hands back — plans one test and runs another. A worker that finds a different
tree than the run was planned from says so and runs nothing.

==============================================================================
HOOKS                                                              *ntf-HOOKS*

A hook module returns an optional `setup` and an optional `teardown`. Which
process runs them, and how often, is what the three flags differ in. A module
that returns anything else, or that carries a key neither hook is read from, is
rejected naming what it returned.

--test-hook=FILE                                               *ntf-hook-test*

Loaded in every worker (via `dofile`). Each test runs in its own worker, so the
functions run once per test, outside everything the spec itself defines:
`setup` before the spec is built, `teardown` after the worker's test has run.
(`before_each`/`after_each` bracket the test body; these bracket the worker.)
>lua
  -- test_hook.lua
  return {
    setup = function() end,
    teardown = function() end,
  }
<
>sh
  ntf --test-hook=./test_hook.lua
<
- A relative path resolves against the working directory (the plugin under
  test).
- An error while loading the module, or from `setup`, is reported as a load
  error.
- A `teardown` error is reported as an error entry alongside the worker's
  results, so it fails the run without discarding what was produced.

--global-hook=FILE                                           *ntf-hook-global*

The same module contract, run once in the launcher process instead of in every
worker: `setup` before any spec file is loaded, `teardown` after all workers
have finished. Use it for state shared by the whole run — start a server once,
build a fixture once — while `--test-hook` remains the per-test bracket. What
it sets in its own process, the runtimepath included, no worker sees — that is
|ntf-hook-process|, which every process runs.
>sh
  ntf --global-hook=./global_hook.lua
<
- An error while loading the module, or from its `setup`, aborts the run before
  any test starts.
- A `teardown` error is reported after the results, and fails the run without
  discarding them.

--process-hook=FILE                                         *ntf-hook-process*

Run in every process ntf starts — the launcher and each worker alike — before
that process loads a spec, which makes it the one hook the two of them agree
on. It is for what a process has to be before it can read a spec at all: the
runtimepath a dependency plugin sits on, a `package.path` entry, a stub. It
takes a `setup` and nothing else, so a module carrying a `teardown` is rejected
naming the two flags it does belong in.
>lua
  -- process_hook.lua
  return {
    setup = function() end,
  }
<
>sh
  ntf --process-hook=./process_hook.lua
<
- The launcher loads every spec file to discover the tests in it, so a module a
  spec requires at file scope has to reach the launcher as well as the workers.
  This is the only hook that reaches both.
- An error while loading the module, or from its `setup`, aborts the run before
  any test starts.

Loading dependency plugins                               *ntf-hook-dependency*

A worker is a clean `nvim`, started with none of your config or plugins, so its
runtimepath holds ntf and the plugin under test (the working directory) and
nothing else. A plugin your specs depend on is put there by `--process-hook`,
whose `setup` runs before a spec is loaded, in every process that loads one:
>lua
  -- dependency_hook.lua
  return {
    setup = function()
      vim.opt.runtimepath:append(vim.fs.joinpath(vim.fn.getcwd(), "deps/dependency"))
      vim.cmd.runtime({ args = { "plugin/**/*.{vim,lua}" }, bang = true })
    end,
  }
<
>sh
  ntf --process-hook=./dependency_hook.lua
<
- The other two hooks cannot do this: each reaches only one of the two
  processes that load a spec.
- The runtimepath alone only makes the dependency's `lua/` and other runtime
  files findable; startup is over, so its `plugin/` scripts are sourced only
  where the second line does it.

Attaching a debugger                                          *ntf-hook-debug*

Because `setup` runs before the spec is built, it is where a debugger attaches:
the code under test then loads with the debugger already on. ntf has no
debugger dependency of its own.
>lua
  -- debug.lua
  return {
    setup = function()
      require("lldebugger").start()
    end,
  }
<
>sh
  ntf --test-hook=./debug.lua --jobs=1 --filter='the test name'
<
Worker stdout is captured, so keep it to a single worker (`--jobs=1`) and
narrow to one test (`--filter`). Wiring the debugger transport itself is up to
your module.

==============================================================================
COVERAGE                                                        *ntf-COVERAGE*

`--coverage` measures line coverage of the code under test while the specs run.
It needs no extra install: each worker loads the code under test with a counter
on every statement of it, and ntf merges the per-worker counts, prints a short
summary, and writes the counts in the `luacov.stats.out` format:
>sh
  ntf --coverage
<
The stats go to a cache file named for the working directory (under
`stdpath("cache")/ntf/coverage/`), so that every project you run ntf in keeps
its own instead of the runs overwriting one another, and
|ntf.coverage.decorate()| reads that same file back with no path to configure.
`--coverage=FILE` writes them where you name instead.

What it measures:

- every file under the working directory, except the test tree: any
  `*_spec.lua` file, and `./spec` when the project has one, so anything sitting
  alongside the specs there, cloned test dependencies say, is left out too. The
  paths you name pick which specs run, never which code is measured
- a measured file no test executed, which shows up at 0%
- a measured file however the run reaches it: `require`, `dofile` and `loadfile`
  all load the counted copy, though a chunk built out of source text by
  `loadstring` is measured no more than the text it was built from
- but never a LuaCATS meta file, which by definition never runs

A run that measured no line at all fails rather than reporting `n/a`.

Not everything under the working directory is code you hold your tests to —
vendored third-party files, your own test helpers, a test tree that is not
`./spec` and so is measured like the rest. `--exclude-code=PATH` leaves a file
or directory out of the code under test; repeat it for each one. The `mutation`
commands take it too, since they measure the same code:
>sh
  ntf --coverage --exclude-code=lua/vendor --exclude-code=lua/mymod/test
<
The built-in summary counts a line only where a statement starts on it, so a
line a statement is merely continued onto counts for neither side of the ratio.
For an authoritative or HTML report, hand the stats to LuaCov — which ntf does
not depend on — by writing them where it looks:
>sh
  luarocks install luacov
  ntf --coverage=luacov.stats.out
  luacov          # reads luacov.stats.out -> luacov.report.out
<

==============================================================================
MUTATION TESTING                                        *ntf-MUTATION-TESTING*

`ntf mutation` measures how much of the covered code your tests actually pin
down. It first runs the specs as usual — a mutant means nothing against a
failing suite, so the run stops there if a test fails — then makes one small
change at a time to the code under test and runs the tests again. A mutant that
makes a test fail is detected; one that leaves the whole suite green is a hole
in the tests, and is reported with the change it got away with:
>sh
  ntf mutation
<
- Each kind of change is an operator: |ntf-MUTATION-OPERATORS| lists them, and
  the `operators` of |ntf-MUTATION-CONFIG| says which of them a run takes.
- Only the mutants a test can reach are run. The mutation commands always
  collect the same line coverage as `--coverage`, and a mutant on a line no
  test executes is reported NO COVERAGE instead of being run.
- A mutant is run against the tests that reach it, in the same
  one-process-per-test isolation as a normal run, and stops at the first test
  that detects it. A test that hangs on a mutant is killed and counts as
  detected.
- The score is the share of detected mutants, counting an uncovered one as
  undetected.
- `--target=PATH` restricts the mutated files to one file or directory, which
  is how you keep a run short: mutating everything means running the suite once
  per mutant.
- The full result — every mutant, its position, and what it became — is written
  to a cache file named for the working directory (under
  `stdpath("cache")/ntf/mutation/`, as `--coverage` files its stats), which
  |ntf.mutation.decorate()| reads back to set the survivors as diagnostics in a
  buffer with no path to configure. `--results=FILE` writes it where you name
  instead.
- A mutant is spliced in when the module is `require`d, so a file the specs
  load through `dofile`/`loadfile` keeps its original source and is reported
  NOT APPLIED — never as a survivor. It leaves the score instead of passing it,
  so a run that reached no code at all scores nothing rather than scoring well.

`--strict` turns the score into a gate, exiting non-zero when any mutant
survived, was left uncovered, or never landed. `--strict=survived` (or
`=no_coverage`, `=not_applied`) gates only that category, so the bar can be
raised in steps:
>sh
  ntf mutation --strict
<
A run that enumerated no mutant at all — a `--target` naming no file to mutate,
or an `exclude` covering every one of them — fails whatever `--strict` was asked
for, its categories counting only the mutants a run did enumerate.

To see what a run would cover before paying for it, `ntf mutation list` runs
the tests once and lists the mutants with the number of tests that reach each:
>sh
  ntf mutation list
<
A listing holds its `--config` to the same account a scoring run does: an entry
of `baseline`, `exclude` or `exclude_spec` that the code no longer holds is
reported and fails the listing.

==============================================================================
MUTATION OPERATORS                                    *ntf-MUTATION-OPERATORS*

An operator is one kind of change. A mutant is reported under its operator's
name, and written into a baseline entry under it too. A name is a verb and what
it acts on, and the verbs are a closed set: `swap-` puts a sibling of the same
kind in place of the original, `drop-` takes something out and leaves what
surrounded it, `force-` pins a decision to one outcome, and `perturb-` shifts a
value by one.

Each has a tag of its own, and is shown with the change its own example gets
and with what a test has to do to detect it.

swap-relational                                          *ntf-swap-relational*
>
  local _ = a <= b -> local _ = a < b
<
  a test has to exercise the boundary value the two disagree on

swap-logical                                                *ntf-swap-logical*
>
  local _ = a and b -> local _ = a or b
<
  a test has to reach operands the two connectives disagree on

swap-arithmetic                                          *ntf-swap-arithmetic*
>
  local _ = a + b * c % d ^ e -> local _ = a - b * c % d ^ e
  local _ = a + b * c % d ^ e -> local _ = a + b / c % d ^ e
  local _ = a + b * c % d ^ e -> local _ = a + b * c * d ^ e
  local _ = a + b * c % d ^ e -> local _ = a + b * c % d * e
<
  a test has to exercise a right operand the two disagree on

  The bitwise operators have no site of this operator, and neither does
  `//`, which the runtime a mutant is loaded in cannot compile.

swap-boolean                                                *ntf-swap-boolean*
>
  local _ = true -> local _ = false
<
  a test has to depend on the value, not merely execute the line

  A condition spelled as a bare `true` or `false` is a site of this operator
  rather than of |ntf-force-branch|, so it is changed once instead of forced
  to each outcome it already names.

drop-not                                                        *ntf-drop-not*
>
  local _ = not a -> local _ = a
<
  a test has to reach the branch the negation decides

drop-negation                                              *ntf-drop-negation*
>
  local _ = -a -> local _ = a
<
  a test has to exercise a nonzero operand and depend on its sign

perturb-number                                            *ntf-perturb-number*
>
  local _ = 1 -> local _ = 2
<
  a test has to depend on the exact value, not on its being non-zero

  A hexadecimal literal is shifted into decimal, so `0x10` becomes `17`.

perturb-length                                            *ntf-perturb-length*
>
  local _ = #a -> local _ = (#a - 1)
<
  a test has to depend on the exact count, not on its being non-zero

  The shifted count is parenthesized, so whatever the expression sits under
  still takes all of it.

force-branch                                                *ntf-force-branch*
>
  if a then end -> if false then end
  if a then end -> if true then end
<
  each outcome needs a test that depends on which side ran

  Only the condition is rewritten, so each mutant keeps the body it decides
  over.

force-loop                                                    *ntf-force-loop*
>
  while a do end -> while false do end
<
  a test has to depend on the body running, which a repeat still does once

  A `while` is pinned to false and a `repeat` to true — the outcome that
  leaves the loop, since the other spins forever to prove nothing a coverage
  hit does not already. A `for` is rewritten to the clause of its own kind
  that iterates none, so its body never runs.

drop-call                                                      *ntf-drop-call*
>
  f() -> do end
<
  a test has to observe what the call does, not merely reach its line

  Only a call standing as a statement of its own: deleting one whose value
  is used would take the expression around it too.

drop-assignment                                          *ntf-drop-assignment*
>
  a.b = c -> do end
<
  a test has to observe what the assignment stores, not merely reach it

  A `local` declaration is left alone, since deleting it would rewrite the
  scope every later mention of the name is read through, and not the store.

drop-return-value                                      *ntf-drop-return-value*
>
  function f() return a end -> function f() return nil end
<
  a test has to depend on what the function answers, not merely reach it

  Only a return that answers for a function: the one a chunk answers with is
  the module itself. A return of a single literal is left to that literal's
  own operator, which already owns the change.

==============================================================================
MUTATION CONFIG                                          *ntf-MUTATION-CONFIG*

Every judgement a project makes about its mutation lives in one policy file,
passed with `--config=FILE`:
>json
  {
    "version": 1,
    "operators": "all",
    "baseline": [
      {
        "path": "lua/mymod.lua",
        "col": 14,
        "operator": "perturb-number",
        "original": "0",
        "replacement": "1",
        "line": "  return n > 0",
        "rationale": "the specs only ever ask about 0, whose answer the shifted boundary leaves false",
        "invariant_spec": "is false at the boundary"
      }
    ],
    "exclude": [
      {
        "path": "lua/launcher.lua",
        "operators": "all",
        "rationale": "runs only in the process that starts the run, which no spec drives, so every one of its mutants comes back uncovered instead of detected"
      }
    ],
    "exclude_spec": [
      {
        "path": "spec/cli_spec.lua",
        "rationale": "drives the built command end to end, so as a trial it pays a whole run to reach code the unit specs already reach in-process"
      }
    ]
  }
<
>sh
  ntf mutation --config=spec/mutation.json
<
One section per kind of judgement, of which only the first two are required:
>
  version       the policy file format, currently 1 (required)
  operators     which operators produce mutants at all (required)
  baseline      the mutants judged impossible to kill
  exclude       files whose mutants the run leaves out
  exclude_spec  specs never picked as a mutant's trial
<
`operators` has no default because no default can say for a project which
operators it holds its tests to:

- `"all"` takes every operator, including the ones a later ntf adds — the right
  answer for a project that upgrades ntf and its own tests together.
- An array of names instead pins the set:

>json
  {
    "version": 1,
    "operators": [
      "swap-relational",
      "swap-logical",
      "swap-boolean",
      "drop-not",
      "perturb-number",
      "force-branch"
    ]
  }
<
  An operator added upstream then reaches the run only once its name is written
  here, so upgrading ntf does not turn a green gate red with survivors nobody
  asked for. Until then its mutants are counted `unadopted` and left out of the
  score.
- A name no operator answers to is rejected before the tests run: a set that
  runs less than it says is the one mistake this file must not keep to itself.

A key none of those sections names — a misspelled `excludes`, say — is rejected
before the tests run, and an entry is held to its own fields the same way.

The other two sections are |ntf-MUTATION-BASELINE| and |ntf-MUTATION-EXCLUDE|.

==============================================================================
MUTATION BASELINE                                      *ntf-MUTATION-BASELINE*

Some mutants are equivalent to the code they replace, which no test can detect.
Rather than re-reviewing those survivors on every run, record each one — with
the reason — in the `baseline` section shown in |ntf-MUTATION-CONFIG|. A listed
mutant is reported `equivalent` and leaves the score, which can then reach 100
and be held there with `--strict`.

An entry names its mutant with the report's own words for it:
>
  path            the mutated file, relative to the working directory
  row             the 1-based line, only where the content names two mutants
  col             the 1-based column the mutant starts at
  operator        the change, named as the report names it
  original        what the mutant replaces
  replacement     what it puts there
  line            the text of the line, matched instead of a row
  rationale       why no test can detect it (required)
  invariant_spec  full name of the test the rationale rests on
  uncovered       true where no test reaches it, so nothing re-runs it
<
So `ntf mutation baseline add` writes the entry for you. It takes the mutant as
the report spells it and the judgement you have to supply, and runs no test —
what it writes is the claim that no test can tell the difference:
>sh
  ntf mutation baseline add --config=spec/mutation.json \
    --mutant=lua/mymod.lua:3:14:perturb-number \
    --rationale='the specs only ever ask about 0, whose answer the shifted boundary leaves false' \
    --invariant-spec='is false at the boundary'
<
- Every report spells a mutant as `PATH:ROW:COL:OPERATOR`, the same name
  `ntf mutation list` prints and the one `--mutant` takes, so naming one is a
  copy rather than a translation.
- `--replacement` is only needed where a single position holds more than one of
  that operator's mutants — the forced branch, which has one per outcome. Such
  a position is answered with the replacements to choose from rather than
  guessed at.

An entry names its mutant by content rather than by position, so it survives the
line moving. Where a file carries the same line twice — same text, same column,
same operator — the content names two mutants and the run fails as AMBIGUOUS
BASELINE. `row` is what separates them, and `baseline add` writes one only
there, an entry that carries one going LOST BASELINE as soon as its line
moves.

An entry is otherwise only ever trusted, not checked: a mutant a new test would
now detect stays out of the score behind a mark that no longer holds.
`ntf mutation baseline verify` runs the listed mutants instead of trusting
them, reporting BASELINE KILLABLE with the test that did it and exiting
non-zero. It leaves every other mutant unrun, scoring nothing and writing no
results file, which is what you want right after editing the baseline:
>sh
  ntf mutation baseline verify --config=spec/mutation.json
<
Verify fails the same way when it re-ran none of the entries its config lists,
so a run scoped past all of them is answered rather than passing as `0/0`. A
kill has to repeat before it counts, so a test that fails for reasons of its own
does not condemn an entry.

`--verify-baseline` asks the same question of a scoring run, verifying the
entries in the same pass that scores the rest, so a gate that wants both answers
pays for one run of the suite instead of two:
>sh
  ntf mutation --config=spec/mutation.json --strict --verify-baseline
<
An entry is re-run by the tests that reach its mutant, so one no test reaches
has nothing to re-run and would otherwise pass by saying nothing at all. Such an
entry makes that second claim itself with `uncovered`, which
`ntf mutation baseline add --uncovered` writes. Both halves are held to the
suite: UNCOVERED BASELINE, once an entry without the mark is reached by no test,
and COVERED BASELINE, once an entry carrying it is reached by one. Reaching the
mutant with a spec is still what a NO COVERAGE mutant asks for; the mark is for
the line a spec cannot reach. Only the whole suite can say that nothing reaches
a mutant, so UNCOVERED BASELINE is answered for only where the run takes every
test the project has.

A rationale usually rests on a fact from somewhere else — what the callers pass,
what shape another module hands over, what the runtime does with a value — and
that fact can stop holding without the marked line moving. `invariant_spec`
names the test that pins it, so renaming or deleting that test fails the run as
UNPINNED BASELINE instead of quietly unmooring the rationale. A run that takes
part of the suite cannot tell a name that is gone from one it never selected, so
that line, like LOST BASELINE, is answered for only where the run takes every
test the project has.

==============================================================================
MUTATION EXCLUDE                                        *ntf-MUTATION-EXCLUDE*

A baseline entry answers for one mutant. A whole file, or a whole spec, is left
out in one of four ways, which are not interchangeable:
>
  --exclude-code=PATH    neither measured nor mutated; takes no reason
  exclude (config)       unmutated only; `--coverage` still measures it
  --exclude-spec=PATH    the spec is not discovered, so it never runs
  exclude_spec (config)  the spec runs, but is never a mutant's trial
<
The flags are the blunt forms: they take no rationale, so such a list grows
quietly and outlives what put each path on it. `--exclude-code` is still what a
vendored copy wants, and a gate built on `--exclude-spec` has to run the suite a
second time to cover what it dropped.

The `exclude` section is for code that only ever executes in a process no spec
drives, where every mutant comes back uncovered rather than detected:
>
  path       the file the entry answers for
  operators  "all", or the operator names to leave out (required)
  rationale  why its mutants are left out (required)
<
Leaving a whole file out is the widest judgement the file can carry, so an
entry spells how much of it is meant:

- `"all"` names the whole file.
- An array of operator names leaves only those operators' mutants out — the
  file stays in the run, and they are reported `excluded` and, like an
  equivalent one, outside the score. That is the entry to write while a file's
  tests are still too coarse for one operator but answer for the rest, and it
  holds while the code is edited, where a baseline entry per surviving mutant
  would go LOST BASELINE on the first move.

The `exclude_spec` section answers for a test rather than for a file under
test:
>
  path       the spec the entry answers for
  rationale  why it is never a trial (required)
<
A mutant is run against the tests that reach it, so an end-to-end spec — one
that drives the whole CLI, or a real editor — is picked as a trial for most of
the code and pays a full run each time to reach what a unit spec already
reaches. Listing its path keeps it out of every trial, and out of the coverage
that decides which mutants are covered at all, so a mutant only it reaches is
reported NO COVERAGE. It still runs with the rest of the suite, and the run
still stops when it fails, which is what lets one mutation run stand in for a
plain test run in CI.

Both sections fail the run — UNUSED EXCLUDE and UNUSED EXCLUDE SPEC — when an
entry covers nothing, so a path that has been renamed, or is already covered by
a broader entry, has to be answered for. An `exclude_spec` entry outside the
spec paths the run was given is left alone, the run never having looked for the
file it names.

==============================================================================
MUTATION REPORT                                          *ntf-MUTATION-REPORT*

A mutation run prints one line per judgement it wants answered, each naming the
mutant as `PATH:ROW:COL:OPERATOR` or the config entry as its path:
>
  TIMEOUT              a hung test detected it, which a busy machine can fake
  FLAKY BATCH          a kill only a shared process made, taken back
  SURVIVED             no test noticed the change
  NO COVERAGE          no test reaches the line, so it was never run
  NOT APPLIED          the file was not `require`d, so nothing changed
  BASELINE KILLABLE    a baseline entry a test kills, named with the test
  LOST BASELINE        a baseline entry whose `line` is no longer there
  AMBIGUOUS BASELINE   its content names two mutants and it carries no `row`
  UNPINNED BASELINE    its `invariant_spec` names no test that passed
  UNCOVERED BASELINE   no test reaches it and it carries no `uncovered`
  COVERED BASELINE     it carries `uncovered`, but a test reaches it
  UNUSED EXCLUDE       an `exclude` entry covering no measurable file
  UNUSED EXCLUDE SPEC  an `exclude_spec` entry covering no spec file
<
The count line above them tallies every mutant, including the ones that leave
the score:
>
  killed             detected: a test failed on the mutant
  timeout            detected: a test hung and its worker was killed
  survived           undetected: the tests stayed green
  no coverage        undetected: no test reached the line
  not applied        the mutant never landed, so it says nothing
  equivalent         a `baseline` entry, left out of the score
  excluded           an `exclude` entry's operator, out of the score
  unadopted          an operator `operators` does not take
  baseline killable  a `baseline` entry a test killed
<
`--strict` gates on the three undetected statuses: survived, no coverage and
not applied. A run that scored no mutant at all says so — `Mutation: n/a (no
mutant scored)` — instead of reading as a run with nothing to do. Everything
else that fails a run — a stale baseline entry, an exclude entry covering
nothing — fails it on its own, with or without the gate.

A mutant is tried against the tests that cover it, cheapest first, sharing one
process until a test fails. That test only names a candidate: every verdict is
taken from trials run one to a process, so what one test leaves behind for the
next never decides a mutant. A kill that does not come back on its own is
reported FLAKY BATCH and taken back, and the trials left are run from a new
process; a mutant no test killed is tried again with every trial in a process
of its own before it is reported survived.

==============================================================================
HIGHLIGHT GROUPS                                        *ntf-HIGHLIGHT-GROUPS*

NtfCoverageCovered                                     *hl-NtfCoverageCovered*
  covered lines

NtfCoverageMissed                                       *hl-NtfCoverageMissed*
  coverable-but-missed lines

==============================================================================
Lua module: ntf                                                      *ntf-ntf*

describe({name}, {fn})                                        *ntf.describe()*
  Define a test group. Its body runs at build time to discover nested
  `describe`/`it`; the body itself is never reported as a test.

  Parameters: ~
    {name} (string) group name
    {fn} (fun()) body that declares nested `describe`/`it`

it({name}, {fn}, {opts})                                            *ntf.it()*
  Define a test case. The body runs at execution time, in its own fresh Neovim
  process. This is not configurable: state never leaks between tests, because
  no two tests ever share a process.
  Its full name has to be its own within the file: |ntf-WRITING-SPECS|.

  Parameters: ~
    {name} (string) test name
    {fn} (fun()) test body
    {opts} (NtfItOption?) |NtfItOption|

pending({name}, {fn})                                          *ntf.pending()*
  Mark a test as pending. As a declaration it records a skipped node; called
  from a running test body or from a before_each it aborts the test as
  pending. Either way the report lists the test on its own `PENDING` line,
  with a reason given from a running test printed below it.
  Calling it from an after_each or a finally never skips the test, the result
  being decided by then; it is reported as an error like any other raise from
  those two.

  Parameters: ~
    {name} (string) pending reason
    {fn} (fun()?) optional body (ignored; pending is never executed)

before_each({fn})                                          *ntf.before_each()*
  Register a hook run before each test in the current group.

  Parameters: ~
    {fn} (fun()) hook body

after_each({fn})                                            *ntf.after_each()*
  Register a hook run after each test in the current group.

  Parameters: ~
    {fn} (fun()) hook body

finally({fn})                                                  *ntf.finally()*
  Register a callback run when the current test finishes (pass or fail).

  Parameters: ~
    {fn} (fun()) callback body

==============================================================================
Lua module: ntf.assert                                        *ntf-ntf.assert*

equal({want}, {got})                                      *ntf.assert.equal()*
  Assert two values are equal (`==`).

  Parameters: ~
    {want} (any) expected value
    {got} (any) actual value

same({want}, {got})                                        *ntf.assert.same()*
  Assert two values are deeply equal.

  Parameters: ~
    {want} (any) expected value
    {got} (any) actual value

truthy({got})                                            *ntf.assert.truthy()*
  Assert `got` is truthy (not `nil` and not `false`).

  Parameters: ~
    {got} (any) value under test

falsy({got})                                              *ntf.assert.falsy()*
  Assert `got` is falsy (`nil` or `false`).

  Parameters: ~
    {got} (any) value under test

is_true({got})                                          *ntf.assert.is_true()*
  Assert `got` is exactly `true`.

  Parameters: ~
    {got} (any) value under test

is_false({got})                                        *ntf.assert.is_false()*
  Assert `got` is exactly `false`.

  Parameters: ~
    {got} (any) value under test

is_nil({got})                                            *ntf.assert.is_nil()*
  Assert `got` is `nil`.

  Parameters: ~
    {got} (any) value under test

match({pattern}, {got})                                   *ntf.assert.match()*
  Assert `got` is a string matching the Lua `pattern`.

  Parameters: ~
    {pattern} (string) Lua pattern
    {got} (any) value under test

register({name}, {fn})                                 *ntf.assert.register()*
  Register a custom assertion onto ntf's assert object.
  Available afterwards as `assert.{name}(...)` and `assert.no.{name}(...)`.

  Parameters: ~
    {name} (string) assertion name
    {fn} (function) factory `function(self) return function(_, args) ... end end` returning the predicate

register_eq({name}, {get_actual})                   *ntf.assert.register_eq()*
  Register an assertion that compares the actual value with `==`.

  Parameters: ~
    {name} (string) assertion name
    {get_actual} (function) maps the leading args to the actual value (last arg is the expected)

register_same({name}, {get_actual})               *ntf.assert.register_same()*
  Register an assertion that compares the actual value with deep equality.

  Parameters: ~
    {name} (string) assertion name
    {get_actual} (function) maps the leading args to the actual value (last arg is the expected)

==============================================================================
Lua module: ntf.coverage                                    *ntf-ntf.coverage*

stats_path({opts})                                 *ntf.coverage.stats_path()*
  The `luacov.stats.out` file `ntf --coverage` writes for a directory, which
  is what `decorate` reads when it is given no path. Resolve it yourself to
  decorate a buffer against a project other than the current directory.

  Parameters: ~
    {opts} (NtfCoverageStatsPathOption?) |NtfCoverageStatsPathOption|

  Return: ~
    (string) 

decorate({opts})                                     *ntf.coverage.decorate()*
  Decorate a buffer's sign column with per-line test coverage read from a
  `luacov.stats.out` file (as written by `ntf --coverage`): covered lines are
  marked with the `NtfCoverageCovered` highlight, coverable-but-missed lines
  with `NtfCoverageMissed`.

  Parameters: ~
    {opts} (NtfCoverageDecorateOption?) |NtfCoverageDecorateOption|

is_decorated({opts})                             *ntf.coverage.is_decorated()*
  Whether `decorate` is currently drawing on the buffer. Intended for a
  toggle mapping paired with `decorate`.

  Parameters: ~
    {opts} (NtfCoverageIsDecoratedOption?) |NtfCoverageIsDecoratedOption|

  Return: ~
    (boolean) 

==============================================================================
Lua module: ntf.helper                                        *ntf-ntf.helper*

find_plugin_root({plugin_name})                *ntf.helper.find_plugin_root()*
  Returns plugin root directory full path.

  Parameters: ~
    {plugin_name} (string) lua module name (`lua/{plugin_name}/*.lua`)

  Return: ~
    (string) plugin root directory full path

get_module_root({module_name})                  *ntf.helper.get_module_root()*
  Returns root module name.
  For example, `get_module_root("plugin_name.module1")` returns `plugin_name`.

  Parameters: ~
    {module_name} (string) lua module name

  Return: ~
    (string) root module name

==============================================================================
Lua module: ntf.mutation                                    *ntf-ntf.mutation*

results_path({opts})                             *ntf.mutation.results_path()*
  The results file `ntf mutation` writes for a directory, which is what
  `decorate` reads when it is given no path. Resolve it yourself to decorate a
  buffer against a project other than the current directory.

  Parameters: ~
    {opts} (NtfMutationResultsPathOption?) |NtfMutationResultsPathOption|

  Return: ~
    (string) 

decorate({opts})                                     *ntf.mutation.decorate()*
  Mark a buffer's surviving mutants, read from the results file (as
  written by `ntf mutation`): each mutant a test let through becomes a warning
  diagnostic in |ntf.mutation.namespace()|, underlining the code it changed
  and carrying what it put there as its message, `ntf` as its source and the
  operator as its code. Detected mutants are not set: they say nothing about
  the tests.

  Parameters: ~
    {opts} (NtfMutationDecorateOption?) |NtfMutationDecorateOption|

is_decorated({opts})                             *ntf.mutation.is_decorated()*
  Whether the buffer holds survivors `decorate` set. Intended for a
  toggle mapping paired with `decorate`.

  Parameters: ~
    {opts} (NtfMutationIsDecoratedOption?) |NtfMutationIsDecoratedOption|

  Return: ~
    (boolean) 

namespace()                                         *ntf.mutation.namespace()*
  The diagnostic namespace `decorate` sets the survivors in. Requiring this
  module gives the namespace a display of its own — the `▌` sign and virtual
  lines (|vim.diagnostic.Opts.VirtualLines|), each survivor taking a line of
  its own — which `vim.diagnostic.config(opts, ntf.mutation.namespace())`
  overrides. It is also what `vim.diagnostic.get()`, `jump()` and
  `setloclist()` take to work with the survivors alone.

  Return: ~
    (integer) 

==============================================================================
STRUCTURE                                                      *ntf-STRUCTURE*

NtfItOption                                                      *NtfItOption*

- {timeout} (integer?) per-process timeout in ms, overriding the
    `--timeout` default; an exceeded worker is killed and reported as an
    error. A mutation run takes it as the ceiling for the test's trials.

NtfCoverageStatsPathOption                        *NtfCoverageStatsPathOption*

- {working_dir} (string?) the directory the run was made from (default:
    the current one).

NtfCoverageDecorateOption                          *NtfCoverageDecorateOption*

- {enable} (boolean?) when `false`, clear the decoration instead of drawing
    it (default `true`).
- {path} (string?) `luacov.stats.out` file to read
    (default: |ntf.coverage.stats_path()|).
- {buffer} (integer?) target buffer (default `0`, the current buffer).

NtfCoverageIsDecoratedOption                    *NtfCoverageIsDecoratedOption*

- {buffer} (integer?) target buffer (default `0`, the current buffer).

NtfMutationResultsPathOption                    *NtfMutationResultsPathOption*

- {working_dir} (string?) the directory the run was made from (default:
    the current one).

NtfMutationDecorateOption                          *NtfMutationDecorateOption*

- {enable} (boolean?) when `false`, clear the decoration instead of drawing
    it (default `true`).
- {path} (string?) mutation results file to read
    (default: |ntf.mutation.results_path()|).
- {buffer} (integer?) target buffer (default `0`, the current buffer).

NtfMutationIsDecoratedOption                    *NtfMutationIsDecoratedOption*

- {buffer} (integer?) target buffer (default `0`, the current buffer).

NtfAssert                                                          *NtfAssert*

- {no} (NtfAssert) the same assertions with the expectation flipped
- {[string]} (fun(...)) custom assertions registered via `ntf.assert.register*`

==============================================================================
vim:tw=78:ts=8:ft=help
